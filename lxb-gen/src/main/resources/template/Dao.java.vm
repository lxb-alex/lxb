package ${package}.dao${tablePrefix};

import org.springframework.stereotype.Repository;
import java.util.List;
import java.util.Map;

import ${package}.entity${tablePrefix}.${className}Entity;

/**
 * ${comments}
 * 
 * @author ${author}
 * @date ${datetime}
 */
@Repository()
public interface ${className}Dao {
    // ======= 自动生成接口 START =========
    int save(${className}Entity entity);

    int saveSelective(${className}Entity entity);

    int save(Map<String, Object> map);

    int saveBatch(List<${className}Entity> list);

    int update(${className}Entity entity);

    int updateByPrimaryKeySelective(${className}Entity entity);

    int update(Map<String, Object> map);

    int delete(Object id);

    int delete(Map<String, Object> map);

    int deleteBatch(Object[] id);

    ${className}Entity get${className}Entity(Object id);

    List<${className}Entity> selectList(Map<String, Object> map);

    List<${className}Entity> selectList(Object id);

    int getTotal(Map<String, Object> map);

    int getTotal();
    // ======= 自动生成接口  END =========
}
