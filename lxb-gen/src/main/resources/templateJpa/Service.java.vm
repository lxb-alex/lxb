package ${package}${tablePrefix}.service;

import ${package}${tablePrefix}.entity.${className};

import java.util.List;
import java.util.Map;

/**
 * ${comments}
 * 
 * @author ${author}
 * @date ${datetime}
 */
public interface ${className}Service {
	

	List<${className}> selectList();

	int save(${className} ${classname});

}
